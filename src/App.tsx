
import { Flex } from 'antd'
import { AppBar } from './AppBar'

export const  App = () => {

  return (
    <Flex vertical style={{background:"transparent", width:"100%"}}>
      <AppBar/>
    </Flex>
  )
}
